import os

category_path = 'categories'

def choose_category(categories):
    category = input("Select a category")
    return category


def create_category():
    category = input("Select a category")
    if
